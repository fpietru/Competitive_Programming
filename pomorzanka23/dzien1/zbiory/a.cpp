#include <bits/stdc++.h>
using namespace std;

constexpr int INF = 1e9+7;
constexpr int MxN = 50005;
int dist[MxN];

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0), cout.tie(0);
	
	int n; cin >> n;
	vector<int> A(n);
	for (auto &ai : A)
		cin >> ai;
		
	for (int i=0; i<
	

	return 0;
}
